const cds = require('@sap/cds')

const major = Number(cds.version.split('.')[0])
if(major > 8) throw new Error('@sap/cds-hana is incompatible with @sap/cds version 9 and higher. Please remove it from your dependencies and add @cap-js/hana instead!')